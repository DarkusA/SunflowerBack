namespace Domain.Entities;

//Esto es un enum, no entidad pero no sabia si ponerlo con Entidadades o crear una carpeta de enums.
public enum Role
{
    User,
    OrderManager,
    Admin,
    SysAdmin
}