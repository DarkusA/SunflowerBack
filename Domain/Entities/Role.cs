namespace Domain.Entities;

//Esto es un enum, no entidad pero no sabia si ponerlo con Entidadades o crear una carpeta de enums en modelos de Application.
public enum Role
{
    User,
    OrderManager,
    Admin,
    SysAdmin
}